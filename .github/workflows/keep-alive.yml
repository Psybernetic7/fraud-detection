name: Keep Streamlit App Awake
on:
  schedule:
    - cron: '0 */6 * * *'  # every 6 hours
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sSf https://psybernetic7-fraud-detection-fraud-detection-ijwqb5.streamlit.app/ || exit 1
